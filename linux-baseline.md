Filesystem and navigation

I have a broad image of the Linux filesystem. I cannot decribe them one by one, but I know that /proc filsytem holds information of the currently running processes. 
For example the /home filesystem is for the user. All the other make part of the root filesystem (/). They also have different filesystem type and block-size.

I can navigate around easily in the terminal with pwd, ls -la, whoami, cd, mv, rm. I can change user/file permissions with chmod and change ownership with chown.

Processes

I can echo processes running in the current terminal session with ps command, get detailed view of system processes with top, know some process alteration command
arguments, basics about bg jobs. I know what a cron job is doing but I am still learning it.

Networking Basics

I understand basic networks well, ip and subnetting some level, dhcp, dns, https, ports, ping, ssh. File transfer.

Bash scripting

I can write simple functions, loops, conditionals. Not a beginner in syntax but I lack writing it myself due to lack of exercise.

Text processing

Used grep many times, piping also.

Package management

Here I have a bit more experience since I installed different linux distros and had to do troubleshooting and setup. I can install ,update, remove.


I ALSO KNOW WHAT IS COMMAND AND PROCESS SUBSTITUTION, LINUX ARCHITECTURE, ROOT PRIVILEGES, SOME SERVICE MANAGEMENT.
